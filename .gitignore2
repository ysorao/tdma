# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignora archivos tipo bundle
/.bundle

# Ignora archivos para base de datos SQLite
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignora conexiones con PostgreSQL
/config/database.yml

# Ignora todos los archivos logs y temporales
/log/*
!/log/.keep
/tmp
/log
/spec
/test
/vendor/bundle
/public/system/*
**.orig
rerun.txt

# Ignora archivos de comprecion de pipeline
*.sassc
.sass-cache
/public/assets/*

# Ignora archivos de pruebas
capybara-*.html
.rspec

# Ignora imagenes subidas con carrierwave
/public/uploads/*

# Ignora archivos del sistema operativo OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Ignora archivos de IDE
.idea
/documentacion

# Ignora otros
*.rbc
.rvmrc
/coverage/
pickle-email-*.html
.project
Desktop.ini
config/database.yml\nconfig/secrets.yml
config/local_env.yml