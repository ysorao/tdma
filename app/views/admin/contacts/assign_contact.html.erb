<div id="todo">
  <%= form_tag(assign_update_admin_contact_index_path(), method: 'put') do %>
    <div class="seccion1">
      <div class="barra_superior1">
        Asignar ticket
      </div>
      <li class="lista"> 
        <label><h2 id="name_esp">Nombre administrador</h2></label>
        <%= select_tag(:other_contact, options_for_select(AdminUser.where(role: AdminUser::SOPORTE_TECNICO).collect {|r| [r.name.to_s + ' ' + r.surnames.to_s, r.id] }), prompt: 'Seleccionar', class: 'select_espe', required: true) %> 
        <%= hidden_field_tag(:contact_id, params[:id]) %>    
      </li>  
    </div> <!--CIERRA LA SECCIÓN--> 
    <div class="botones1">
       <%= button_tag(type: "submit", class:"btn btn-primary btn-lg") do %>
         Asignar
        <% end %>
       <a class="boton_cancelar1" href="/admin/contacts">
         Cancelar
       </a>
    </div>
  <%end%>
</div>